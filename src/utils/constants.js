import { INFO } from "../../config/project/project.env";
// 常量
const CONST = { PAGESIZE: 15 };
const STRING = {
    INFO: INFO,
    TOKEN: "$token-desktop-tab",
    HOME: "home"
};
export { CONST, STRING };
